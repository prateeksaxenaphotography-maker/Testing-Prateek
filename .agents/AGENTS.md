# Workspace Rules

- Always verify features/functionality implemented in Wolverine Photo Studio and ensure they are matched and replicated across all other portfolios (Lukas, Matthias, Nordic) automatically, without needing explicit prompts.
